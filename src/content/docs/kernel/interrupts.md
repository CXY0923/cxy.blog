---
title: 中断与调度
description: 中断描述符表与协作式调度的实现取舍。
date: 2026-05-22
category: 内核
tags: [内核]
---

## 中断描述符表

IDT 每一项都是"别慌"的预案:入口地址、特权级、门类型在编译期就写死,运行时只做查表跳转。未注册的向量统一落到一个"打日志然后 halt"的兜底门,宁可停机也不带着未知的态继续跑。

## 协作式调度

协作式调度换来确定性,代价是信任每一个任务都必须显式让出 CPU。任何路径上不允许出现无界循环,怀疑自己会跑久的任务应当主动调用 yield——调度器不负责拯救不让出的人。
